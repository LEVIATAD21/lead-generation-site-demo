export const services = ['Diagnóstico inicial','Planejamento técnico','Acompanhamento remoto'];
export function validateRequest(data) { return Boolean(data.name?.trim()) && /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(data.email || '') && data.consent === 'on'; }
export function mount(root) { root.innerHTML = `<section class="hero"><article class="hero-card"><p class="eyebrow">Institucional demonstrativo</p><h1>Um caminho simples entre entender o serviço e pedir orçamento.</h1><p class="lede">Estrutura de geração de contatos com consentimento explícito. Não envia dados nem representa uma empresa existente.</p></article><aside class="hero-card"><p class="eyebrow">Cobertura ilustrativa</p><h2>Atendimento remoto e regional</h2><p class="muted">Áreas e serviços devem ser definidos pelo responsável real.</p></aside></section><section class="grid two"><article class="card"><h2>Serviços</h2><div class="list">${services.map((service) => `<div class="item"><b>${service}</b><br><span class="muted">Descrição demonstrativa e revisável.</span></div>`).join('')}</div></article><article class="card"><h2>Solicitar conversa</h2><form id="request" class="stack"><label class="field">Nome<input name="name" /></label><label class="field">E-mail<input name="email" type="email" /></label><label class="field"><span><input type="checkbox" name="consent" /> Concordo com o uso local destes dados apenas para esta demonstração.</span></label><button class="button">Validar solicitação</button><p id="message" class="muted"></p></form></article></section>`; root.querySelector('#request').addEventListener('submit', (event) => { event.preventDefault(); const data = Object.fromEntries(new FormData(event.currentTarget)); const ok = validateRequest(data); const out = root.querySelector('#message'); out.className = `result ${ok ? '' : 'error'}`; out.textContent = ok ? 'Solicitação validada localmente. Nenhuma informação foi enviada.' : 'Preencha nome, e-mail válido e consentimento.'; }); }